[Unit]
Description=ASUS G14 GPU boot script...
Before=graphical.target
After=suspend.target

[Service]
User=root
Type=simple
ExecStart=/usr/sbin/asus_gpu_boot

[Install]
WantedBy=graphical.target
WantedBy=suspend.target
